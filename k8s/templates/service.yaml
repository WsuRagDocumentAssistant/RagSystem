apiVersion: v1
kind: Service
metadata:
  name: rag-server
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: rag-server
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP
